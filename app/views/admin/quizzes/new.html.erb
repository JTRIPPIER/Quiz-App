<%= form_for([:admin, @quiz], :html => { :class => 'form' }) do |f| %>

<div class="form-group">
  <%= f.label :title %>
  <%= f.text_field :title, class: 'form-control' %>
</div>

<div class='form-group'>
    <%= f.submit 'Save', class: 'btn btn-success' %>
</div>
<% end %>
